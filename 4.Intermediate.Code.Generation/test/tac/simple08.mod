module simple08;

var i : integer;
    j, j0, j1, j2 : boolean;
    k : boolean;
    l, l0 : boolean;

begin

    i := (i + 1);
    j := (j0 && j1) && j2;
    j := j0 && j1;
    i := i + i;
    k := (i > 2);
    l := (l = l0);

    return

end simple08.
