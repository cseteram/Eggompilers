module simple15;

var i, j, k, l, n, m : boolean;
    ret : boolean;

begin
    ret := (i = j) || (k # l) && (!j) || (!n && (n || m)) && true || (!true && (i && (j || (k && (l || (!l || l))))));
    return
end simple15.
