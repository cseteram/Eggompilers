module instr00;

var a,b,c : integer;
    d0,d1,d2 : boolean;

begin

    // binary operations
    a := b + c; // opAdd
    a := b - c; // opSub
    a := b * c; // opMul
    a := b / c; // opDiv
    d0 := d1 && d2; // opAnd
    d0 := d1 || d2 // opOr

end instr00.
